---
name: Stale

# yamllint disable-line rule:truthy
on:
  schedule:
    - cron: "0 8 * * *"
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

jobs:
  workflows:
    # zizmor: ignore[unpinned-uses] - reusable workflow intentionally uses @main
    uses: hassio-addons/workflows/.github/workflows/stale.yaml@main
